spring:
  datasource:
    url: jdbc:mysql://localhost:3306/prueba?useSSL=false&serverTimezone=UTC
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    defer-datasource-initialization: true
    # Deshabilita la búsqueda de persistence.xml
    persistence-unit-name: default